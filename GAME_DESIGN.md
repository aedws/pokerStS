# pokerStS — 게임 설계 문서 (Game Design Document)

> **한 줄 소개**: 포커 족보로 데미지를, 주사위 탄환으로 속성을 정하는 덱빌딩 로그라이크.
> 슬레이 더 스파이어(구조) × 발라트로(포커 점수) × 주사위 탄환(속성/무기) 을 결합한 웹 싱글플레이 게임.

---

## 1. 콘셉트 개요

플레이어는 **총잡이(Gunslinger)** 가 되어 층을 오르며 적을 처치한다.
매 공격은 두 축으로 결정된다.

| 축 | 담당 | 비유 |
|----|------|------|
| **포커 족보** | 데미지의 **크기** (숫자) | "얼마나 세게 쏘는가" — 계획/실력 |
| **주사위 탄환** | 데미지의 **속성** (불/얼음/번개…) | "어떤 총알을 장전했는가" — 빌드/운 |

즉, 손패로 좋은 족보를 만들어 **높은 데미지**를 뽑고, 장전한 탄환(주사위)의 **속성**으로 그 데미지에 상성·부가효과를 입힌다. 무기마다 탄창 규칙이 달라 **"어떤 총 + 어떤 탄환 + 어떤 카드 빌드"** 를 맞추는 것이 이 게임의 덱빌딩 핵심이다.

### 핵심 판타지
> "불속성 탄환만 꽉 채운 리볼버로, 플러시를 완성해 화염 6연발을 꽂아넣는다."

---

## 2. 핵심 전투 루프 (Combat Loop)

한 번의 공격(사격)은 다음 순서로 진행된다.

```
1. 손패 확인      → 트럼프 카드 5~8장을 손에 든다
2. 카드 선택      → 족보를 이룰 카드를 고른다 (버리기/다시 뽑기 가능)
3. 족보 판정      → 족보 등급 → 기본 데미지(칩 × 배수) 산출
4. 탄창 확인      → 무기 슬롯에 장전된 주사위(탄환)들
5. 주사위 굴림    → 각 탄환의 속성/눈이 확정
6. 속성 적용      → 데미지에 속성 상성·부가효과 반영 → 발사
7. 적 반응        → 피해, 상태이상, 반격
8. 재장전/드로우  → 다음 턴 준비
```

### 데미지 계산 공식 (초안)

```
최종 데미지 = ( 족보_기본칩 + Σ선택카드_숫자값 ) × 족보_배수 × 속성_배수 × 크리티컬
```

- **족보_기본칩 / 족보_배수**: 아래 4장 족보표 참조
- **속성_배수**: 적의 약점/저항 + 장전 탄환 속성 조합으로 결정 (5장)
- **크리티컬**: 주사위 눈 조건(예: 같은 눈 2개 이상) 충족 시 발동

---

## 3. 포커 족보표 (데미지 담당)

발라트로식 "칩 × 배수" 를 쓴다. 값은 밸런싱용 초안이며 튜닝 대상.

| 족보 | 조건 | 기본 칩 | 배수 | 비고 |
|------|------|--------:|-----:|------|
| 하이카드 | 아무 조합 없음 | 5 | ×1 | 최소 보장 |
| 원페어 | 같은 숫자 2장 | 10 | ×2 | |
| 투페어 | 페어 2쌍 | 20 | ×2 | |
| 트리플 | 같은 숫자 3장 | 30 | ×3 | |
| 스트레이트 | 연속 5장 | 30 | ×4 | |
| 플러시 | 같은 무늬 5장 | 35 | ×4 | 속성 빌드와 시너지 |
| 풀하우스 | 트리플+페어 | 40 | ×4 | |
| 포카드 | 같은 숫자 4장 | 60 | ×7 | |
| 스트레이트 플러시 | 연속+동일무늬 | 100 | ×8 | |
| 로열 플러시 | 10~A 동일무늬 | 100 | ×8 | 연출용 최상위 |

- **선택 카드 숫자값**: J=11, Q=12, K=13, A=14 (또는 1). 칩에 합산.
- **무늬 ↔ 속성 연동(선택 규칙)**: 스페이드=번개, 하트=화염, 다이아=냉기, 클럽=독 처럼 무늬가 속성 시너지를 주는 옵션도 검토(무기/유물로 해금).

---

## 4. 주사위 탄환 시스템 (속성 담당)

### 4.1 탄환(Bullet) = 주사위
- 탄환 하나 = **주사위 1개**. 주사위는 **속성**과 **면(눈)** 을 가진다.
- 무기의 **탄창 슬롯**에 탄환을 장전한다. 공격 시 장전된 주사위를 모두 굴린다.
- 눈(1~6)은 **속성 강도/부가효과 트리거**로 쓰인다. (높은 눈일수록 강한 속성 효과)

### 4.2 속성 종류 (초안)

| 속성 | 색 | 부가효과 컨셉 | 상성(강함) |
|------|----|--------------|-----------|
| 🔥 화염 (Fire) | 빨강 | 화상(도트 데미지) | 대상: 얼음/식물 계열 |
| ❄️ 냉기 (Ice) | 파랑 | 둔화/빙결(행동 지연) | 대상: 화염 계열 |
| ⚡ 번개 (Lightning) | 노랑 | 감전(다음 피해 증폭) | 대상: 물/금속 계열 |
| ☠️ 독 (Poison) | 초록 | 중첩 도트, 방어 무시 | 대상: 생물 계열 |
| ⚪ 무속성 (Normal) | 회색 | 상성 없음, 안정적 | — |

- 적은 **약점 속성(×1.5~2 피해)** / **저항 속성(×0.5 피해)** / **면역** 을 가질 수 있다 → 탄환 선택이 전략이 됨.
- **속성 조합 콤보**: 예) 냉기로 빙결 → 화염으로 "열충격" 추가 피해. (해금형 시너지)

### 4.3 주사위 눈 → 부가효과 (예시 룰)

```
같은 눈 2개 이상  → 크리티컬 (×1.5)
같은 눈 3개 이상  → 강크리티컬 (×2) + 속성 부가효과 2배
합계 눈이 높을수록 → 속성 게이지 축적 (필살기 자원)
눈 1 (탄피 걸림)   → 해당 탄환 이번 턴 불발 (리스크)
```

---

## 5. 무기 시스템 (탄창 규칙 담당)

무기는 **탄창 슬롯 수 + 장전 규칙 + 발사 방식** 을 정의한다. 무기가 곧 빌드의 방향성.

| 무기 | 슬롯(주사위 수) | 장전 규칙 | 발사 방식 | 성향 |
|------|:--:|-----------|-----------|------|
| 리볼버 (Revolver) | 6 | 자유 혼합 | 단일 대상 · 크리 특화 | 밸런스/크리 |
| 소총 (Rifle) | 3 | **단일 속성만** (예: 불만) | 단일 대상 · 속성 배수 ↑ | 속성 특화 |
| 샷건 (Shotgun) | 4 | 자유 혼합 | **전체 대상** 분산 | 광역 |
| 기관총 (SMG) | 8 | 저눈 위주 | 다연발 · 낮은 개별 피해 | 연사/도트 |
| 매그넘 (Magnum) | 2 | 고급 탄환만 | 초고배수 단발 | 한방/포카드 빌드 |

- **"단일 속성 장전"** 규칙: 예시로 주신 *"소총에 불속성 탄환만 꽉 채우기"* 를 구현하는 규칙. 조건 충족 시 **속성 특화 보너스**(예: 화염 배수 +50%) 발동.
- 무기는 상점/보상으로 교체·강화. 무기 강화 = 슬롯 추가, 재장전 속도, 크리 조건 완화 등.

---

## 6. 덱빌딩 & 리소스

플레이어는 **세 가지 풀**을 동시에 키운다.

1. **카드 덱 (Playing Deck)** — 포커 족보용 트럼프. 카드 추가/제거/강화(에디션·강화카드)로 원하는 족보가 잘 나오게 튜닝.
2. **탄환 풀 (Bullet Pool)** — 보유한 주사위(속성별). 어떤 속성을 얼마나 확보했는지가 무기 궁합을 좌우.
3. **유물 (Relics)** — 상시 패시브. 예:
   - "플러시 완성 시 화염 탄환 1발 자동 장전"
   - "같은 눈 3개면 골드 획득"
   - "매 전투 첫 사격은 반드시 크리티컬"

### 자원
- **HP**: 피격 시 감소, 0이면 런 종료.
- **골드**: 상점 재화.
- **에너지/탄창**: 턴당 사격/재장전 횟수 제한(전투 템포 조절).

---

## 7. 로그라이크 구조 (슬더슬 뼈대)

- **분기 맵**: 노드를 선택해 위로 진행.
  - ⚔️ 일반 전투 · ☠️ 엘리트 · ❓ 이벤트 · 🛒 상점 · 🔥 휴식(회복/강화) · 👑 보스
- **Act 구성**: 3개 Act, 각 Act 끝에 보스. 클리어 시 다음 Act.
- **보상 루프**: 전투 → 카드/탄환/유물/골드 중 선택 보상.
- **영구 해금(메타)**: 런 반복으로 새 무기·속성·유물·이벤트 해금.
- **난이도(승천/Ascension)**: 클리어할수록 상위 난이도 개방.

---

## 8. 기술 스택 & 아키텍처 (웹)

- **프론트엔드 단독으로 완성 가능** (서버 불필요, 싱글플레이).
- 스택 제안:
  - **React + TypeScript + Vite** (빠른 개발/빌드)
  - **Zustand** — 게임 상태(런/전투/덱) 관리
  - **Tailwind CSS** — UI 스타일
  - **Framer Motion** — 카드·주사위 애니메이션
- **저장**: `localStorage` (진행상황·해금·설정).
- **순수 로직 분리**: `src/engine/` 에 규칙 엔진(족보 판정, 데미지 계산, 주사위, 적 AI)을 UI와 분리 → 테스트 용이.
- (선택) 온라인 랭킹·클라우드 세이브가 필요해지면 그때 **Supabase** 연동.

### 폴더 구조(안)
```
src/
  engine/        # 순수 게임 규칙 (UI 무관, 유닛테스트 대상)
    poker.ts     # 족보 판정 + 칩/배수
    dice.ts      # 주사위/탄환 굴림
    damage.ts    # 최종 데미지 계산
    combat.ts    # 전투 상태머신
    enemies.ts   # 적 정의/AI
  state/         # Zustand 스토어 (런/전투/메타)
  ui/            # React 컴포넌트 (전투화면/맵/상점/손패/주사위)
  data/          # 무기·속성·유물·적·이벤트 데이터(JSON/TS)
  assets/
```

---

## 9. MVP 범위 (1차 목표)

가장 먼저 **"전투 1판이 재미있게 도는가"** 를 검증한다.

**MVP 포함:**
- 전투 화면 1개: 손패 → 족보 선택 → 주사위 굴림 → 속성 데미지 → 적 처치
- 무기 1~2종(리볼버 + 소총), 속성 3종(화염/냉기/번개)
- 족보 판정 + 데미지 공식 동작
- 적 2~3종(약점/저항 포함)
- 승리/패배 판정

**MVP 이후(2차):**
- 분기 맵 + 여러 전투 + 보상 루프
- 상점 · 유물 · 이벤트
- 무기/속성 확장, 메타 해금, 밸런싱

---

## 10. 열린 결정 사항 (다음에 정할 것)

- [ ] 무늬(스페이드/하트…)를 속성과 직접 연동할지 여부
- [ ] 턴당 사격 횟수/에너지 규칙의 구체 수치
- [ ] 주사위 눈 → 부가효과 룰 최종안
- [ ] 아트 방향(웨스턴 총잡이? SF? 도트? 미니멀?)
- [ ] 제목/브랜딩 (임시: **pokerStS**)

---

*본 문서는 초안이며 구현/플레이테스트로 수치와 규칙을 조정한다.*
