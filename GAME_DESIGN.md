# pokerStS — 게임 설계 문서 (Game Design Document)

> **한 줄 소개**: 카드에 속성이 박힌 탄환을 주사위로 장전하고, 포커 족보로 데미지를 뽑는 덱빌딩 로그라이크.
> 슬레이 더 스파이어(구조) × 발라트로(포커 점수) × 총·탄환(속성/무기) 을 결합한 웹 싱글플레이 게임.

---

## 1. 콘셉트 개요

플레이어는 **총잡이(Gunslinger)** 가 되어 층을 오르며 적을 처치한다.
세 가지 시스템이 맞물려 한 번의 사격을 만든다.

| 시스템 | 담당 | 비유 |
|--------|------|------|
| **포커 족보** | 데미지의 **크기** (숫자) | "얼마나 세게 쏘는가" — 계획/실력 |
| **카드 속성(무늬)** | 데미지의 **속성** (불/얼음/번개…) | "어떤 총알인가" — 빌드 |
| **주사위(1D6)** | **장전 수 + 선공 + 데미지 보너스** | "얼마나 장전됐고, 누가 먼저 쏘는가" — 운/템포 |

즉, **카드 = 탄환**이다. 카드마다 숫자(포커용)와 속성(무늬)이 함께 박혀 있다.
매 턴 **주사위를 굴려 이번 턴에 장전되는 탄환(=손패) 수**를 정하고, 그 손패로 **족보를 완성해 데미지**를 뽑으며, 사용한 카드들의 **속성**으로 상성·부가효과를 입힌다.

### 핵심 판타지
> "불속성 탄환을 잔뜩 장전한 리볼버로, 하트 플러시를 완성해 화염 폭발을 꽂아넣는다."

### 왜 이 구조인가 (설계 의도)
- **속성이 카드에** 있으므로 플레이어는 고르는 물건 하나(카드)만 보면 된다 → 직관적.
- **무늬 = 속성**이라 포커 규칙과 속성 빌드가 저절로 시너지 (플러시 = 단일 속성 대폭발).
- **주사위 = 장전 수**로 "총에 탄환을 장전한다"는 판타지와 운/템포 레버를 동시에 유지.

---

## 2. 핵심 전투 루프 (Combat Loop)

한 번의 턴은 다음 순서로 진행된다.

```
1. 장전(주사위)   → 1D6 굴림 → 장전 수 + 선공 여부 + 데미지 보너스 결정
2. 손패 확인      → 장전 수만큼 탄환(카드)을 손에 든다
3. 카드 선택      → 족보를 이룰 카드를 고른다 (재장전 시 2로 복귀, 턴 행동 수 내)
4. 족보 판정      → 족보 등급 → 기본 데미지(칩 × 배수) 산출
5. 속성 판정      → 사용 카드 무늬 비율 → 공격 속성(비례 분배) 결정
6. 발사          → 낮은 눈이면 적보다 먼저(선공), 데미지+속성 부가효과 명중
7. 적 반응        → 생존 시 반격 (선공으로 처치하면 반격 무효)
8. 턴 종료        → XP 정산 / 다음 턴 준비
```

### 데미지 계산 공식 (초안)

```
장전 수(손패 크기) = 주사위 굴림 결과  (무기 탄창 상한 내로 클램프)

최종 데미지 = ( 족보_기본칩 + Σ선택카드_숫자값 ) × 족보_배수 × 속성_배수 × 크리티컬
```

- **족보_기본칩 / 족보_배수**: 3장 족보표 참조
- **속성_배수**: 사용 카드 무늬 조합 + 적의 약점/저항으로 결정 (4장)
- **크리티컬**: 주사위 같은 눈 반복/유물 조건 충족 시 (5.3 참조)
- **선공(이니셔티브)**: 낮은 주사위 눈이면 적 행동 **전에** 사격 → 상태이상 선적용/선제 처치로 반격 무효 (5.3·8 참조)

---

## 3. 포커 족보표 (데미지 담당)

발라트로식 "칩 × 배수" 를 쓴다. 값은 밸런싱용 초안이며 튜닝 대상.

| 족보 | 조건 | 기본 칩 | 배수 | 비고 |
|------|------|--------:|-----:|------|
| 하이카드 | 아무 조합 없음 | 5 | ×1 | 최소 보장 |
| 원페어 | 같은 숫자 2장 | 10 | ×2 | |
| 투페어 | 페어 2쌍 | 20 | ×2 | |
| 트리플 | 같은 숫자 3장 | 30 | ×3 | |
| 스트레이트 | 연속 5장 | 30 | ×4 | |
| 플러시 | 같은 무늬 5장 | 35 | ×4 | **단일 속성 대폭발** |
| 풀하우스 | 트리플+페어 | 40 | ×4 | |
| 포카드 | 같은 숫자 4장 | 60 | ×7 | |
| 스트레이트 플러시 | 연속+동일무늬 | 100 | ×8 | |
| 로열 플러시 | 10~A 동일무늬 | 100 | ×8 | 연출용 최상위 |

- **선택 카드 숫자값**: J=11, Q=12, K=13, A=14 (또는 1). 칩에 합산.

---

## 4. 카드 속성 시스템 (속성 담당)

### 4.1 카드 = 탄환
- 카드 1장 = 탄환 1발. 카드는 **숫자(포커용)** 와 **속성(무늬)** 을 동시에 가진다.
- 공격에 **사용한 카드들의 무늬**가 그 사격의 속성을 결정한다.
- **단일 무늬로 몰수록**(예: 플러시) 속성 효과가 극대화된다 → 포커·속성 시너지의 핵심.

### 4.2 무늬 ↔ 속성 매핑

| 무늬 | 속성 | 색 | 부가효과 컨셉 | 상성(강함) |
|------|------|----|--------------|-----------|
| ♥ 하트 | 🔥 화염 (Fire) | 빨강 | 화상(도트 데미지) | 얼음/식물 계열 |
| ♦ 다이아 | ❄️ 냉기 (Ice) | 파랑 | 둔화/빙결(행동 지연) | 화염 계열 |
| ♠ 스페이드 | ⚡ 번개 (Lightning) | 노랑 | 감전(다음 피해 증폭) | 물/금속 계열 |
| ♣ 클럽 | ☠️ 독 (Poison) | 초록 | 중첩 도트, 방어 무시 | 생물 계열 |

- **혼합 무늬 사용 시 = 속성 비례 분배**: 족보에 **실제 사용된 카드들의 무늬 비율**대로 데미지를 쪼개 각 속성으로 동시 발사한다.
  - 예) 사용 카드 3♥ + 2♠ → 총 데미지의 **60% 화염 / 40% 번개**. 각 조각은 자기 속성의 상성으로 따로 계산된다.
  - 이러면 적의 약점 속성 비중을 손패로 조절하는 미세 플레이가 생긴다.
- **단일 무늬 보너스**: 사용 카드가 전부 같은 무늬면 100% 단일 속성 + **속성_배수 +50%** (플러시/속성 빌드 보상).
- 적은 **약점 속성(×1.5~2)** / **저항 속성(×0.5)** / **면역**을 가질 수 있어 카드 선택이 전략이 된다.
- **속성 콤보(해금형)**: 냉기로 빙결 → 화염으로 "열충격" 추가 피해 등.

### 4.3 카드 강화(에디션)
- 특정 카드에 속성 강화(예: "이 하트는 화염 배수 +25%"), 숫자 강화, 와일드(무늬 자유) 등 부여 → 덱빌딩 깊이.

---

## 5. 주사위 & 장전 시스템 (탄창 담당)

### 5.1 기본 주사위 = 1D6
- 턴/사격마다 **주사위(기본 1D6)** 를 굴린다. 이 한 번의 굴림이 세 가지를 동시에 좌우한다:

| 굴림 결과 | 좌우하는 것 | 방향 |
|-----------|-------------|------|
| **눈 값** | **장전 수**(= 손에 드는 카드 수) | 높을수록 많이 장전 |
| **눈 값** | **선공(이니셔티브)** | **낮을수록** 적보다 빠르게 행동 |
| **눈 값** | **비례 데미지 보너스** | **높을수록** 데미지 증가 |

> **핵심 긴장**: 낮게 굴리면 **빠르지만 적게**(선공, 적은 손패·데미지), 높게 굴리면 **세지만 느리게**(많은 손패·데미지, 후공). 매 굴림이 도박이 된다.

### 5.2 주사위 성장 (레벨업 · 보스)
- **레벨업**: 굴릴 때마다 **특성(주사위 면 개조)** 을 부여할 수 있다. 예) 특정 면에 크리 확정, 재장전 보너스, 속성 각인 등.
- **보스 처치**: 주사위의 **앞숫자(최소면) 또는 뒷숫자(최대면)** 를 증감할 수 있다.
  - 예) 뒷숫자 +1 → 1~6 이 1~7 로 (고점↑, 대박 장전 가능) / 앞숫자 +1 → 2~6 으로 (하한↑, 안정적이나 선공 약화).
  - 앞/뒤 조절로 **"안정형 주사위 vs 한탕형 주사위"** 빌드가 갈린다.

### 5.3 눈 보너스 룰 (확정)

```
같은 숫자 반복(재장전/재굴림에서 동일 눈) → 크리티컬
낮은 숫자                                → 선공(적보다 먼저 행동)
높은 숫자                                → 눈에 비례해 데미지 증가
```

- 재장전은 주사위를 다시 굴린다. **연속으로 같은 눈**이 나오면 크리티컬 발동(로우리스크·하이리턴 도박).

### 5.4 선공(이니셔티브)의 효과
- **낮은 눈으로 선공**하면 **적 공격 이전에** 사격이 발생한다:
  - **상태이상 선적용**: 화상/빙결/감전/독을 적 행동 전에 걸어 이번 적 턴부터 효과 발동(빙결이면 적 행동 자체를 지연/무효).
  - **선제 피해**: 사격으로 적을 먼저 처치하면 그 적의 **반격이 무효**.
- 그래서 낮은 눈은 "적은 장전·낮은 데미지" 대신 **"먼저 때린다"** 는 명확한 보상을 갖는다 → 높은 눈(고데미지·후공)과의 트레이드오프 완성.

---

## 6. 무기 시스템 (탄창 규칙 담당)

무기는 **탄창 상한 + 주사위 + 발사 방식 + 속성 규칙** 을 정의한다. 무기가 곧 빌드 방향.

주사위는 공통 **1D6**(5.1~5.2). 무기는 **탄창 상한 + 발사 방식 + 속성 규칙**과, 주사위 굴림 해석의 소폭 변형을 정의한다.

| 무기 | 탄창 상한 | 발사 방식 | 속성 규칙/성향 |
|------|:--:|-----------|----------------|
| 리볼버 (Revolver) | 6 | 단일 대상 · 크리 특화 | 자유 혼합 · 밸런스 |
| 소총 (Rifle) | 5 | 단일 대상 · 속성 배수↑ | **단일 무늬면 대폭 보너스**(불만 채우기) |
| 샷건 (Shotgun) | 5 | **전체 대상** 분산 | 자유 혼합 · 광역 |
| 기관총 (SMG) | 8 | 다연발 · 개별 피해 낮음 | 도트/감전 누적 · 재장전 횟수↑ |
| 매그넘 (Magnum) | 4 | 초고배수 단발 | 포카드/한방 · 높은 눈 데미지 보너스↑ |

- **"단일 속성 장전"**: 예시로 주신 *"소총에 불속성 탄환만 꽉 채우기"* → 손패를 전부 같은 무늬로 맞추면 소총이 속성 특화 보너스를 준다.
- 무기는 상점/보상으로 교체·강화 (탄창 상한↑, 재장전 완화, 크리 조건 완화 등).

---

## 7. 덱빌딩 & 리소스

플레이어는 **두 축**을 키운다.

1. **카드 덱 (탄환 덱)** — 숫자 + 속성(무늬)이 박힌 트럼프. 카드 추가/제거/강화로 원하는 **족보 + 속성**이 잘 나오게 튜닝. (예: 하트 편중 덱 → 화염 플러시 빌드)
2. **유물 (Relics)** — 상시 패시브. 예:
   - "손패가 전부 하트면 화염 배수 +30%"
   - "더블(같은 눈) 시 골드 획득"
   - "매 전투 첫 사격은 반드시 크리티컬"

### 자원
- **HP**: 피격 시 감소, 0이면 런 종료.
- **골드**: 상점 재화.
- **턴당 행동 수(재장전/사격)**: **기본 1** (1D6 콘셉트), **최대 6**. 성장으로 늘린다.
  - 한 번의 행동 = **재장전**(주사위 재굴림 + 손패 갱신, 같은 눈 반복 시 크리 노림) 또는 **사격**(손패 족보로 발사).
  - 행동 수가 늘수록 좋은 족보가 나올 때까지 재장전하거나 여러 번 사격하는 운영이 가능해진다.

---

## 8. 레벨업 & 성장 (Progression)

### 8.1 경험치(XP) — 기본값 (초안)
- 적을 처치하면 XP 획득:

| 적 등급 | 획득 XP |
|---------|--------:|
| 일반 | +10 |
| 엘리트 | +30 |
| 보스 | +100 |

- **레벨업 필요 XP**: `필요치(N→N+1) = 50 + (N-1)×25` — 완만한 상승 곡선.
  - 예) Lv1→2 = 50, Lv2→3 = 75, Lv3→4 = 100, Lv4→5 = 125 …
  - (밸런싱 대상. 너무 빠르면 고정 100/레벨로 교체 검토.)

### 8.2 보상 구조

| 시점 | 보상 | 효과 |
|------|------|------|
| **전투 승리** | **카드 선택** | 제시된 후보 중 1장을 덱에 추가 (덱/속성 풀 확장) |
| **레벨업** | **특성 강화 ↔ 카드 선택 (택1)** | 주사위 특성 강화(5.2) 또는 카드 추가 중 선택 → 빌드 방향 결정권 |
| **10레벨 단위**(10/20/30…) | **무기 강화** | 탄창 상한↑·재장전 완화·크리 조건 완화 등 (6장) |

- **전투 승리 = 덱 확장** — 매 전투 후 카드 1장 선택으로 원하는 족보/무늬(속성)를 강화.
- **레벨업 = 택1 선택지** — "주사위(운·행동 축)를 키울지, 덱(족보·속성 축)을 키울지" 매 레벨 고민하게 만든다.
- **10레벨 = 무기 강화** — 큰 단위 파워스파이크. 무기 성향(6장)이 한 단계 진화.

### 8.3 성장 축 요약
```
주사위 축  ← 레벨업(특성 강화) · 보스(앞뒤숫자 증감)   : 운/행동/선공/데미지 보너스
덱 축      ← 전투 승리(카드) · 레벨업(카드)             : 족보 크기 · 무늬(속성)
무기 축    ← 10레벨 단위 · 상점/보상 교체                : 탄창·발사방식·속성 규칙
유물 축    ← 엘리트/이벤트/상점                          : 상시 패시브
```

---

## 9. 로그라이크 구조 (슬더슬 뼈대)

- **분기 맵**: 노드를 선택해 위로 진행.
  - ⚔️ 일반 전투 · ☠️ 엘리트 · ❓ 이벤트 · 🛒 상점 · 🔥 휴식(회복/강화) · 👑 보스
- **Act 구성**: 3개 Act, 각 Act 끝에 보스. 클리어 시 다음 Act.
- **보상 루프**: 전투 → 카드/유물/골드/무기 중 선택 보상.
- **영구 해금(메타)**: 런 반복으로 새 무기·유물·이벤트·카드 해금.
- **난이도(승천/Ascension)**: 클리어할수록 상위 난이도 개방.

---

## 10. 기술 스택 & 아키텍처 (웹)

- **프론트엔드 단독으로 완성 가능** (서버 불필요, 싱글플레이).
- 스택 제안:
  - **React + TypeScript + Vite**
  - **Zustand** — 게임 상태(런/전투/덱) 관리
  - **Tailwind CSS** — UI
  - **Framer Motion** — 카드·주사위 애니메이션
- **저장**: `localStorage` (진행상황·해금·설정).
- **순수 로직 분리**: `src/engine/` 에 규칙 엔진을 UI와 분리 → 테스트 용이.
- (선택) 온라인 랭킹·클라우드 세이브 필요 시 **Supabase** 연동.

### 폴더 구조(안)
```
src/
  engine/        # 순수 게임 규칙 (UI 무관, 유닛테스트 대상)
    poker.ts     # 족보 판정 + 칩/배수
    dice.ts      # 주사위 → 장전 수
    element.ts   # 무늬→속성, 상성/배수
    damage.ts    # 최종 데미지 계산
    combat.ts    # 전투 상태머신
    enemies.ts   # 적 정의/AI
  state/         # Zustand 스토어 (런/전투/메타)
  ui/            # React 컴포넌트 (전투/맵/상점/손패/주사위)
  data/          # 무기·유물·적·이벤트 데이터(TS/JSON)
  assets/
```

---

## 11. MVP 범위 (1차 목표)

가장 먼저 **"전투 1판이 재미있게 도는가"** 를 검증한다.

**MVP 포함:**
- 전투 화면 1개: 주사위 장전 → 손패 → 족보 선택 → 속성 데미지 → 적 처치
- 무기 1~2종(리볼버 + 소총), 속성 4종(무늬 = 화염/냉기/번개/독)
- 족보 판정 + 데미지 공식 + 무늬→속성 상성 동작
- 적 2~3종(약점/저항 포함)
- 승리/패배 판정

**MVP 이후(2차):**
- 분기 맵 + 여러 전투 + 보상 루프
- 상점 · 유물 · 이벤트
- 무기 확장, 메타 해금, 밸런싱

---

## 12. 아트 방향 (확정: SF 카우보이)

- **레퍼런스**: 데스티니: 가디언즈 — 포세이큰(Forsaken) 계열. **SF 웨스턴/스페이스 카우보이**.
- **무드**: 낡은 가죽·황무지 개척지 + 홀로그램/네온/에너지 무기의 하이테크 대비. 어둡고 거친 프론티어 톤에 강한 속성 발광(화염 주황, 냉기 시안, 번개 노랑, 독 초록).
- **UI**: 탄창/실린더를 형상화한 주사위 위젯, 카드는 총알/탄피 모티프, 홀로그램 HUD 프레임.
- **연출**: 속성별 총구 화염·타격 이펙트, 크리 시 슬로우 연출, 플러시(단일 속성)는 화면 전체 속성 발광.

## 13. 열린 결정 사항 (다음에 정할 것)

- [x] 혼합 무늬 → **속성 비례 분배** (족보 사용 카드 비율대로)
- [x] 주사위 → **1D6**, 눈이 장전 수·선공(낮을수록)·데미지(높을수록)를 좌우, 레벨업 특성/보스 앞뒤숫자 증감
- [x] 눈 보너스 → **같은 눈 반복=크리, 낮음=선공, 높음=데미지↑**
- [x] 턴당 행동 수 → **기본 1, 최대 6**
- [x] 아트 → **SF 카우보이(데스티니 포세이큰 st)**
- [x] 레벨업 → **XP(일반10/엘리트30/보스100), 승리=카드, 레벨업=특성/카드 택1, 10레벨=무기 강화**
- [x] 선공 효과 → **적 공격 이전 사격(상태이상 선적용/선제 처치 시 반격 무효)**
- [ ] 상태이상 상세 수치(화상 도트량, 빙결 지속, 감전 증폭률, 독 중첩 상한)
- [ ] 주사위 특성 풀 구체안(레벨업으로 얻는 면 개조 목록)
- [ ] 제목/브랜딩 (임시: **pokerStS**)

---

*본 문서는 초안이며 구현/플레이테스트로 수치와 규칙을 조정한다.*
